import React from "react";
import STRINGS from "../constants/strings";

const SearchBar = () => (
  <div>
    <input placeholder={STRINGS.HEADER.SEARCH_BAR_PLACEHOLDER} />
    <button type="button">
      <img src="" alt="" />
    </button>
  </div>
);

export default SearchBar;
